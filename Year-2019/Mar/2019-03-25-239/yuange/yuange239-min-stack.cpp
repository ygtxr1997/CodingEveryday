// 也有想过像最小栈那样，构造一个最小队列，能在O(1)的时间获取队列最大值.
// 可以结合最小栈+栈实现队列，但实际上速度并不快，写起来还很复杂
// 因为用栈实现队列的时候，队列的插入时间复杂度是O(k)，所以该方法复杂度是O(nk)
class Solution {
public:
    vector<int> maxSlidingWindow(vector<int>& nums, int k) {
        
    }
};